/*
 * Martello Notes — site-level JavaScript overrides.
 *
 * The theme loads this file whenever `user_overrides: true` is set in
 * _config.yml, after its own bundle. This example needs no extra behaviour —
 * the file exists so the request resolves instead of 404ing, and so there is
 * an obvious place to add site-specific JS.
 */
