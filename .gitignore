_site/
_site_proxycheck/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
/vendor/
# Note: We COMMIT Gemfile.lock for reproducible builds
# See: docs/systems/ZERO_PIN_STRATEGY.md
# Gemfile.lock  # <- REMOVED: lockfile must be committed
env-variables.log
*.DS_Store
assets/.DS_Store

# Environment files (NEVER commit secrets!)
.env
.env.local
.env.*.local
*.env
!.env.example

# Vendor assets (downloaded at build time via scripts/vendor-install.sh)
assets/vendor/

# Gem development
*.gem
/*.gem
pkg/
tmp/
.rspec_status
assets/.DS_Store

# Test framework results and reports
test/results/*.json
test/reports/*
test/coverage/*
logs/*

# Front Matter CMS
.frontmatter/
.vendor/
